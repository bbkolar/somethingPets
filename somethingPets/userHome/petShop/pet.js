/**
 * Pet class used to store pet information in a JSON object. Not used in current version of website. 
 * 
 * Update if use status changes
 */

class Pet {
    $petId;
    $userId;
    $petName;
    $petType;

    constructor() {
        
    }
}